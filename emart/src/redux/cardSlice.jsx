import {createSlice} from "@reduxjs/toolkit";

const initialState ={
    products:[],
}

const cardSlice = createSlice({
    name:'card',
    initialState,
    reducers:{
        

    },
   
})


export default cardSlice.reducer